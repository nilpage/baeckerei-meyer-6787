// Scaffold stub. Replace this file before deploying a per-lead site.
//
// The sanity-scaffold repo is the seed for every per-lead Sanity demo
// under nopage/sanity-generator. Each lead's design is bespoke —
// designed from blank by the agent after walking the audit. There is
// no shared template render. If you are reading this on a deployed
// site, the design step was skipped and the site must not ship.
//
// To unblock: replace `app/page.tsx`, `app/globals.css`, and the
// `sanity/schemaTypes/` directory with the bespoke design for the
// specific lead this repo is for. See
// /home/pi/projects/nopage/sanity-generator/CLAUDE.md, section
// "Per-lead procedure", steps 4–5.

export default function ScaffoldStub() {
  throw new Error(
    "sanity-scaffold: app/page.tsx is a stub. Replace it with a bespoke design for this lead before deploying.",
  );
}
